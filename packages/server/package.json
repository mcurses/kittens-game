{
  "name": "@kittens/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --coverage",
    "test:watch": "vitest",
    "lint": "biome check ./src",
    "dev": "bun --watch src/index.ts"
  },
  "dependencies": {
    "@kittens/api-spec": "workspace:*",
    "@kittens/engine": "workspace:*",
    "@kittens/shared": "workspace:*",
    "drizzle-orm": "^0.45.1",
    "hono": "^4.6.20",
    "lz-string": "^1.5.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.11",
    "@vitest/coverage-v8": "^2.1.8",
    "drizzle-kit": "^0.31.10",
    "typescript": "*",
    "vitest": "^2.1.8"
  }
}
