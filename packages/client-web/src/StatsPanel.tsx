import type React from "react";

export function StatsPanel(): React.ReactElement {
  return (
    <div data-testid="stats-panel">
      <p className="panel-empty">Stats panel parity shell.</p>
    </div>
  );
}
