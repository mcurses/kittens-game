{
  "name": "@kittens/cli",
  "version": "0.1.0",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "bin": {
    "kittens": "./bin/kittens.ts"
  },
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "test": "vitest --run --reporter=verbose src",
    "dev": "bun run bin/kittens.ts",
    "gen:asset": "bun --env-file=../../.env src/generate-asset-gemini.ts"
  },
  "dependencies": {
    "commander": "^11.1.0",
    "chalk": "^5.3.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3",
    "vitest": "^1.0.4"
  }
}
