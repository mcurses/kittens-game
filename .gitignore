node_modules/
**/node_modules/
dist/
coverage/
.turbo/
*.tsbuildinfo
.env
.env.*
!.env.example
*.db
*.db*
*.bak
.DS_Store

# Asset generation lives on the design-systems-core branch (and eventually a
# separate repo). Main ships only the optimized webp exports under
# assets/exports/ — never the raw generations or the pipeline.
assets/higgsfield/
kittens-game-design-system/
packages/cli/src/generate-asset-gemini.ts
packages/cli/src/generate-kitten-prompts.ts
scripts/check-css-coverage.mjs
.stylelintrc.json

# Per-developer working directory. Anything in here is local-only —
# savestate JSON exports, Higgsfield prompt drafts, pipeline experiments,
# personal notes. NEVER pushed to main. See CLAUDE.md and
# local-example/README.md for the canonical layout.
local/
